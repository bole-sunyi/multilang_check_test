  - name: "【操作】01"
    action: "click"
    fallback_pos: [0.7598, 0.918]
    sleep_after: 1  # 第 1 步点击后等待 1 秒，再截图，便于观察界面反馈
    snapshot_after: true

  - name: "【操作】02"
    action: "click"
    fallback_pos: [0.2002, 0.5078]
    sleep_after: 1  # 第 2 步点击后等待 1 秒，再截图，便于观察界面反馈
    snapshot_after: true

  - name: "【操作】03"
    action: "click"
    fallback_pos: [0.5127, 0.5924]
    sleep_after: 1  # 第 3 步点击后等待 1 秒，再截图，便于观察界面反馈
    snapshot_after: true

  - name: "【操作】04"
    action: "click"
    fallback_pos: [0.5117, 0.7708]
    sleep_after: 1  # 第 4 步点击后等待 1 秒，再截图，便于观察界面反馈
    snapshot_after: true

  - name: "【操作】05"
    action: "click"
    fallback_pos: [0.8867, 0.8763]
    sleep_after: 1  # 第 5 步点击后等待 1 秒，再截图，便于观察界面反馈
    snapshot_after: true

  - name: "【操作】06"
    action: "click"
    fallback_pos: [0.8721, 0.293]
    sleep_after: 1  # 第 6 步点击后等待 1 秒，再截图，便于观察界面反馈
    snapshot_after: true
